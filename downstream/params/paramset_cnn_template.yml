

shape_code  : '512_256'
mpp_code    : 'mpp=0.50'
feat_code   : 'swav_resnet50'

method_code : 'cnn'

model_kwargs: # other field will be populated according to the crawling from paramdist
  'nr_types'  : 2
  'backbone'  : 'swav_resnet50'

optimizer_kwargs:
  'lr' : 1.0e-5
  'betas' : [0.9, 0.999]
  'weight_decay' : 0.0

loader_kwargs:
  train: { 'batch_size' : 8, 'nr_procs' : 8}
  infer: { 'batch_size' : 8, 'nr_procs' : 8}